import { StyleSheet } from 'react-native'; 

const MapStyles = StyleSheet.create({
    map: {
        width: '100%',
        height: '100%',
        position: 'absolute',
        // zIndex: '2',
    },

    mapButton: {
        width: 10,
        height: 10,
    }
});

export default MapStyles
